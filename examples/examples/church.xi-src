-- Church Numerals
-- Demonstrates: higher-order functions, lambda calculus encoding

def zero f x = x
def one f x = f x
def two f x = f (f x)
def three f x = f (f (f x))

def church_add m n f x = m f (n f x)
def church_mul m n f x = m (n f) x

def to_int n = n (λx. x + 1) 0

def main = to_int (church_add (church_mul two three) one)
-- Result: 7 (2*3 + 1)
