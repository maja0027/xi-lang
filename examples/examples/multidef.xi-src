-- Multi-definition program
-- Demonstrates: def with parameters, mutual references, let bindings

def min a b = if a < b then a else b
def max a b = if a > b then a else b
def clamp lo hi x = max lo (min hi x)
def abs x = if x < 0 then 0 - x else x

def main = clamp 0 100 (abs (0 - 42))
-- Result: 42
